[package]
name = "bmo"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
gpui = "0.2.2"
gpui-component = "0.4.0-preview1"
rand = "0.9.2"
rust-embed = "8.9.0"
sqlx = {version="0.8.6", features=["runtime-async-std", "sqlite", "chrono"]}

[profile.release]
lto = "thin"
strip = "symbols"

[profile.release-distro]
inherits = "release"
codegen-units = 1
lto = "fat"
opt-level = "s"

[package.metadata.bundle]
name = "Bmo"
identifier = "com.rubbietheone.bmo"
icon = []
